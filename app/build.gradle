plugins {
    id 'buildlogic.java-application-conventions'
}

dependencies {
    implementation 'org.apache.commons:commons-text'
    implementation project(':lib')
}

application {
    mainClass = 'com.cynaptec.connectconsole.App'
}

tasks.named('run') {
    standardInput = System.in  // Redirect standard input to the terminal
}